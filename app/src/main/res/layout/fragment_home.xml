<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/homeRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#F1B765"
    tools:context=".ui.HomeFragment">

    <!-- Video Card occupying the top half (or 50%) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cubeVideoCard"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.5"
        app:layout_constraintWidth_percent="0.95"
        app:cardCornerRadius="16dp"
        app:cardElevation="4dp"
        android:backgroundTint="@color/white">

        <VideoView
            android:id="@+id/cubeVideoView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:pivotX="0.5"
            android:pivotY="0.5"
            android:scaleX="2.0"
            android:scaleY="2.0"
            android:contentDescription="Cube video" />
    </com.google.android.material.card.MaterialCardView>

    <!-- LinearLayout for Filter Spinners (placed immediately below the video card) -->

    <!-- Country TextView below the filters -->

    <!-- Flag ImageView below the country text -->

    <LinearLayout
        android:id="@+id/linearLayoutFilters"
        android:layout_width="386dp"
        android:layout_height="97dp"
        android:layout_marginTop="16dp"

        android:orientation="vertical"
        android:padding="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.571"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cubeVideoCard">

        <!-- Spinner for Difficulty -->

        <!-- Spinner for Dish Type -->
        <Spinner
            android:id="@+id/spinnerDifficulty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/spinner_bg"
            android:textAlignment="center"
            android:fontFamily="@font/my_font"/>

        <Spinner
            android:id="@+id/spinnerTypeDish"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/spinner_bg"
            android:textAlignment="center"
            android:fontFamily="@font/my_font"/>

    </LinearLayout>

    <TextView
        android:id="@+id/textViewCountry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Country will be shown here"
        android:textAppearance="?attr/textAppearanceHeadline6"
        android:textColor="#FFFFFF"
        android:fontFamily="@font/my_font"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/linearLayoutFilters" />

    <ImageView
        android:id="@+id/flagImageView"
        android:layout_width="100dp"
        android:layout_height="60dp"
        android:layout_marginTop="8dp"
        android:contentDescription="Country flag"
        android:scaleType="fitCenter"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/textViewCountry" />

</androidx.constraintlayout.widget.ConstraintLayout>
